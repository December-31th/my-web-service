{
  "name": "ffmpeg-api",
  "version": "1.0.0",
  "description": "Simple FFmpeg merge API (concat 2x15s video + 30s audio)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.19.2",
    "ffmpeg-static": "^5.2.0",
    "fluent-ffmpeg": "^2.1.2"
  }
}
